
export const environment = {
    userPoolId: 'eu-central-1_{USER_POOL_ID}',
    userPoolClientId: '{USER_POOL_CLIENT_ID}',
    API: `https://{API_ID}.execute-api.eu-central-1.amazonaws.com`,
    STAGE: `dev` // or 'prod' based on deployment
}
